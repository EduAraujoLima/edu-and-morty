import { EmptyDirective } from './empty';

describe('Empty', () => {
  it('should create an instance', () => {
    const directive = new EmptyDirective();
    expect(directive).toBeTruthy();
  });
});
